{
  "name": "note-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prestart": "npm run build",
    "build": "npm install && tsc -p .",
    "start": "node dist/app.js",
    "start-dev": "nodemon"
  },
  "dependencies": {
    "express": "^4.18.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "nodemon": "^3.0.3",
    "typescript": "^5.3.3"
  }
}
